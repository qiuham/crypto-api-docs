name: okx
language: zh
type: spa  # 单页应用

crawler:
  start_url: "https://www.okx.com/docs-v5/zh/"

  selectors:
    # 侧边栏链接（包含锚点）
    sidebar_links: 'nav a[href*="#"]'

    # 主内容区
    main_content: "main, article, .content, [role='main']"

    # 标题
    title: "h1, h2"

output:
  base_dir: "docs"
  dir_name: "okx"
